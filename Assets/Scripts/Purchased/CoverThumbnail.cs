using UnityEngine;
using UnityEngine.UI;

namespace Purchased {
    public class CoverThumbnail {
        public string URL;
        public string Title;

        public CoverThumbnail(string url, string title) {
            URL = url;
            Title = title;
        }
    }
}
